# Copiar a filtering/cluster_paths.env y ajustar si tus rutas difieren.
#   cp filtering/cluster_paths.local.env.example filtering/cluster_paths.env
#
# Uso (Git Bash en Windows):
#   source filtering/cluster_paths.env
#   bash filtering/run_filtering_pipeline.sh

export REPO_ROOT="C:/Users/pipel/OneDrive/Escritorio/MAPBIOMAS/CURSOR/fire"
export PYTHON="python"

# --- LULC: un solo GeoTIFF multibanda (bandas hasta 2024) ---
export LULC_STACK="D:/flepin/lulc_collection02/lulc_2025_subset_mosaic_bbox_without_region5.tif"
# Año de la banda 1 del stack (verificar con: python -c "import rasterio; s=rasterio.open(r'...'); print(s.count)")
export START_YEAR_BAND1=2000

# --- Clasificados de fuego ---
export CLASSIFIED_DIR="D:/flepin/classification_20260528"

# --- Salidas locales ---
export WORK_ROOT="D:/flepin/filtering_work_local"

# Años con tiles clasificados a filtrar (incluye 2025 si copiaste clasificación 2024→2025)
export FROM_YEAR=2013
export TO_YEAR=2025

# Último año presente en el stack LULC (solo hasta 2024 en tu mosaic)
export LULC_TO_YEAR=2024

# Duplicar máscaras temáticas y total 2024 → 2025 para filtrar tiles 2025
export COPY_MASK_2025_FROM_2024=1

export WORKERS=4
export STEPS="all"
